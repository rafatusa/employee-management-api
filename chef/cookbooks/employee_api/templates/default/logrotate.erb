# Managed by Chef — local edits are overwritten on the next converge.
<%= @log_dir %>/*.log /var/log/nginx/employee-api.*.log {
    daily
    rotate 14
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
    su root root
    create 0640 root adm
}
